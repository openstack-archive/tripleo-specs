============
Installation
============

At the command line::

    $ pip install tripleo-specs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tripleo-specs
    $ pip install tripleo-specs